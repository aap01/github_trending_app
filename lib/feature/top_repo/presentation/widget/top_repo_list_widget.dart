import 'package:flutter/material.dart';

class TopRepoListWidget extends StatelessWidget {
  const TopRepoListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return SliverToBoxAdapter(child: Container());
  }
}
